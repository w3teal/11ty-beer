<div class="large-space"></div>
<footer class="padding">

    <nav class="row m l">
      <div>
        <h3><a href="{{ site.url }}">{{ site.title }}</a></h3>
        <p class="text-sm">{{ site.description }}</p>
      </div> 
      <div class="max"></div>
      <nav>
        {% for item in site.socialItems %}
        <a class="button circle transparent" href="{{ item.url }}">
          <i>{{ item.icon }}</i>
          <div class="tooltip">{{ item.title }}</div>
        </a>
        {% endfor %}
      </nav>
    </nav>

    <div class="s">
      <div>
        <h3><a href="{{ site.url }}">{{ site.title }}</a></h3>
        <p class="text-sm">{{ site.description }}</p>
      </div>
      <div class="max"></div>
      <nav>
        {% for item in site.socialItems %}
        <a class="button circle transparent" href="{{ item.url }}">
          <i>{{ item.icon }}</i>
          <div class="tooltip">{{ item.title }}</div>
        </a>
        {% endfor %}
      </nav>
    </div>

  </div>
</footer>